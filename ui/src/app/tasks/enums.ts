export enum Priority {
  Low = "low",
  Medium = "medium",
  High = "high",
}

export enum Status {
  Backlog = "backlog",
  Todo = "todo",
  InProgress = "in progress",
  Done = "done",
  Canceled = "canceled",
}
